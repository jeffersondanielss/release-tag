// @flow

/**
 * Exibe mensagem de ajuda para o usuário
 * 
 */

const help = (): string => `
  Commands

  - release
  - release --help
  - release -v
  - release --version
`

module.exports = help
