// @flow

/**
 * Exibe mensagem de ajuda para o usuário
 * 
 * @returns {string}
 */

const help = () => `
  Commands

  - release
  - release --help
  - release -v
  - release --version
`

module.exports = help
